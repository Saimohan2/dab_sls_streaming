# This is a Databricks asset bundle definition for dab_sls_streaming.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_sls_streaming
  uuid: 6d7a9141-259a-4c90-bcc0-138124a14178

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405604619098106.6.azuredatabricks.net
    variables:
      catalog: dab_sales_streaming_project
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://adb-7405604619098106.6.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/saiadabala@jayveer.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/saiadabala@jayveer.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dab_sales_streaming_project
      schema: prod
    permissions:
      - user_name: saiadabala@jayveer.onmicrosoft.com
        level: CAN_MANAGE
